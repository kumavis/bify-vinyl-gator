{
  "dependencies": {
    "requirejs": "^2.3.6",
    "through2": "^4.0.2",
    "vinyl": "^2.2.1"
  },
  "devDependencies": {
    "@metamask/eslint-config": "^4.0.0",
    "ava": "^3.15.0",
    "browserify": "^17.0.0",
    "eslint": "^7.7.0",
    "eslint-plugin-import": "^2.22.0",
    "eslint-plugin-node": "^11.1.0",
    "vinyl-fs": "^3.0.3"
  },
  "scripts": {
    "example": "rm -rf dist/* && node example/build.js",
    "lint": "eslint .eslintrc.js 'src/*.js' 'example/*.js' 'test/*.js'",
    "lint:fix": "yarn lint --fix",
    "test": "yarn test:unit && yarn lint",
    "test:unit": "ava 'test/*.js'"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
